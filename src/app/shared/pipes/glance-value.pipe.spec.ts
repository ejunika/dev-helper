import { GlanceValuePipe } from './glance-value.pipe';

describe('GlanceValuePipe', () => {
  it('create an instance', () => {
    const pipe = new GlanceValuePipe();
    expect(pipe).toBeTruthy();
  });
});
